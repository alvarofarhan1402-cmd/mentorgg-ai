var API='http://127.0.0.1:8080';
async function loadLeaderboard(){var res=await fetch(API+'/api/leaderboard');var data=await res.json();var list=document.getElementById('leaderboardList');list.innerHTML='';data.data.forEach(function(user){var item=document.createElement('div');item.className='leaderboard-item';item.innerHTML='<span class=rank-badge>#'+user.rank+'</span><span class=player-name>'+user.username+'</span><span class=player-rank>'+user.user_rank+'</span>';list.appendChild(item);});}
async function loadComments(){var res=await fetch(API+'/api/comments');var data=await res.json();var list=document.getElementById('commentsList');list.innerHTML='';data.data.forEach(function(c){var avatar=c.username[0].toUpperCase();var time=new Date(c.time).toLocaleString('id-ID');var item=document.createElement('div');item.className='discord-message';item.innerHTML='<div class=discord-avatar>'+avatar+'</div><div class=discord-content><div class=discord-header><span class=discord-username>'+c.username+'</span><span class=discord-time>'+time+'</span></div><div class=discord-text>'+c.comment+'</div></div>';list.appendChild(item);});}
async function addComment(){var u=document.getElementById('commentUsername').value||localStorage.getItem('username')||'Anon';var c=document.getElementById('commentText').value;if(!c){alert('Tulis komentar dulu!');return;}fetch(API+'/api/comments',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:u,comment:c})}).then(r=>r.json()).then(data=>{if(data.status==='success'){document.getElementById('commentText').value='';loadComments();}else{alert('Gagal kirim!');} });}
window.onload=function(){loadLeaderboard();loadComments();document.getElementById('commentUsername').value=localStorage.getItem('username')||'';};
